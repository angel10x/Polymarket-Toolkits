
.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

config.yaml

.env

# Cargo.lock IS tracked: this is a binary crate, so the lockfile is what makes
# release builds and `cargo build --locked` reproducible across machines and CI.
